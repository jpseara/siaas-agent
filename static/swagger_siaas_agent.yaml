openapi: 3.0.3
info:
  version: 1.0.0
  title: "SIAAS Local API"
  description: "API running in the SIAAS Agent for read-only purposes"

paths:
  /:
    get:
      tags:
        - "index"
      summary: "API information"
      description: "Shows details about the local API running in the agent"
      responses:
        '200':
          description: "Success"
  /siaas-agent:
    get:
      tags:
        - "siaas-agent"
      summary: "Gets agent data"
      description: "Shows module and configuration data from the local DBs"
      parameters:
        - name: module
          in: query
          explode: false
          required: false
          description: "Filters specific modules (accepts multiple values, comma-separated)"
          allowReserved: true
          schema:
            type: array
            items:
              type: string
              enum: ["platform","neighborhood","portscanner","config","*"]
            default: "*"
          #example: "portscanner,neighborhood" # comment to avoid: https://github.com/swagger-api/swagger-ui/issues/5776
      responses:
        '200':
          description: "Success"
        '500':
          description: "Bad input or server error"
